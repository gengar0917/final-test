version: 0.0
os: linux

files:
  - source: /
    destination: /home/ubuntu/finalproject-12BE

permissions:
  - object: /home/ubuntu/finalproject-12BE

    owner: ubuntu
    group: ubuntu
hooks:
  AfterInstall:
    - location: scripts/deploy.sh
      timeout: 60
      runas: ubuntu
